---
title: macOS
description: Installation of Git on macOS.
---



### On macOS

#### Using Xcode Command Line Tools
    ```sh
    xcode-select --install
    ```
<Note type="success" title="Recommended">
  This is the recommended method for most users as it's officially supported by Apple.
</Note>

    This command will open a dialog box to install the command line tools. Follow the instructions to install the tools.

#### Using Homebrew
    ```sh
    brew install git
    ```
    If you don't have Homebrew installed, you can install it by running:

    ```sh
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"
    ```

#### Using MacPorts
    ```sh
    sudo port install git
    ```
    If you don't have MacPorts installed,

  Visit <Link href="https://www.macports.org/install.php">MacPorts website</Link> to download and install the package for your macOS version.


### Verifying the Installation

After installation, you can verify the installation by running:

```sh
git --version
```

Or

```sh
git -v
```

Visit the following resources to learn more:

<Tabs defaultValue="article">
<TabsList>
<TabsTrigger value="article">Articles</TabsTrigger>
<TabsTrigger value="video">Videos</TabsTrigger>
<TabsTrigger value="official">Official Docs</TabsTrigger>
</TabsList>
#### Free Resources
<TabsContent value="article">
<Note type="note" title="article">
- <Link href="https://www.macports.org/install.php">MacPorts website</Link>
</Note>
</TabsContent>
<TabsContent value="video">
<Note type="note" title="video" >
- 
</Note>
</TabsContent>
<TabsContent value="official">
<Note type="note" title="official" >
- 
- 
</Note>
</TabsContent>
</Tabs>

<Outlet path="2-git-basics/installation/macOS" />
