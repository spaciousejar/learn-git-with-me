---
title: Git Add
description: Learn how git add moves changes from your working directory to the staging area, preparing them for commit. Master the staging process with visual explanations and practical examples.
---

The `git add` command is one of the most fundamental Git commands. It **stages changes** from your working directory, preparing them for the next commit. Think of it as telling Git "these are the changes I want to include in my next snapshot."

## How Git Add Works

<GitWorkflowDiagram type="basic" />

The staging area (also called "index") is a crucial concept in Git:

<Mermaid 
  chart={`
    graph LR
      subgraph "Your Computer"
        WD[Working Directory<br/>📁 Modified files]
        SA[Staging Area<br/>📋 Prepared changes]
        LR[Local Repository<br/>💾 Committed snapshots]
      end
      
      WD -->|git add| SA
      SA -->|git commit| LR
      
      style WD fill:#e3f2fd
      style SA fill:#fff3e0
      style LR fill:#e8f5e8
  `}
/>

## Visual Understanding: File States

<Mermaid 
  chart={`
    graph TB
      subgraph "File Lifecycle"
        Untracked[📄 Untracked<br/>New files Git doesn't know about]
        Modified[📝 Modified<br/>Changed files in working directory]
        Staged[✅ Staged<br/>Changes ready for commit]
        Committed[💾 Committed<br/>Changes saved in repository]
      end
      
      Untracked -->|git add| Staged
      Modified -->|git add| Staged
      Staged -->|git commit| Committed
      Committed -->|modify file| Modified
      
      style Untracked fill:#ffcdd2
      style Modified fill:#fff3e0
      style Staged fill:#c8e6c9
      style Committed fill:#e1f5fe
  `}
/>

## Command Syntax and Examples

### Add Single File
```sh
git add <filename>
```

<Mermaid 
  chart={`
    graph LR
      File[📄 index.html] -->|git add index.html| Staging[📋 Staging Area]
      
      subgraph "Before"
        File
      end
      
      subgraph "After"
        Staging
      end
      
      style File fill:#fff3e0
      style Staging fill:#c8e6c9
  `}
/>

### Add All Files in Current Directory
```sh
git add .
```

<Mermaid 
  chart={`
    graph TB
      subgraph "Working Directory"
        F1[📄 index.html - Modified]
        F2[📄 style.css - Modified]
        F3[📄 script.js - New]
        F4[📄 README.md - Modified]
      end
      
      Arrow[git add .]
      
      subgraph "Staging Area"
        S1[✅ index.html]
        S2[✅ style.css]
        S3[✅ script.js]
        S4[✅ README.md]
      end
      
      F1 --> Arrow
      F2 --> Arrow
      F3 --> Arrow
      F4 --> Arrow
      Arrow --> S1
      Arrow --> S2
      Arrow --> S3
      Arrow --> S4
      
      style F1 fill:#fff3e0
      style F2 fill:#fff3e0
      style F3 fill:#ffcdd2
      style F4 fill:#fff3e0
      style Arrow fill:#e3f2fd
      style S1 fill:#c8e6c9
      style S2 fill:#c8e6c9
      style S3 fill:#c8e6c9
      style S4 fill:#c8e6c9
  `}
/>

### Add All Files (Including Deleted)
```sh
git add -A
```
or
```sh
git add --all
```

### Add All Files in a Directory
```sh
git add <directory>/
```

<Mermaid 
  chart={`
    graph TB
      subgraph "Project Structure"
        Root[📁 project/]
        SrcDir[📁 src/]
        TestDir[📁 tests/]
        DocsDir[📁 docs/]
        
        Root --> SrcDir
        Root --> TestDir
        Root --> DocsDir
        
        SrcDir --> F1[📄 app.js]
        SrcDir --> F2[📄 utils.js]
        TestDir --> F3[📄 test.js]
        DocsDir --> F4[📄 README.md]
      end
      
      Command[git add src/]
      
      subgraph "Result: Only src/ files staged"
        S1[✅ src/app.js]
        S2[✅ src/utils.js]
        NotStaged1[❌ tests/test.js]
        NotStaged2[❌ docs/README.md]
      end
      
      SrcDir --> Command
      Command --> S1
      Command --> S2
      
      style F1 fill:#fff3e0
      style F2 fill:#fff3e0
      style F3 fill:#f8f9fa
      style F4 fill:#f8f9fa
      style S1 fill:#c8e6c9
      style S2 fill:#c8e6c9
      style NotStaged1 fill:#ffecb3
      style NotStaged2 fill:#ffecb3
  `}
/>

## Interactive Staging

### Add Parts of a File
```sh
git add -p <filename>
```

This allows you to stage only specific chunks of changes within a file:

<Mermaid 
  chart={`
    graph TB
      subgraph "File with Multiple Changes"
        Change1[🔴 Function A - Modified]
        Change2[🟡 Function B - Modified]
        Change3[🔵 Function C - Modified]
      end
      
      Interactive[git add -p filename.js]
      
      subgraph "Interactive Choices"
        Y[y - Stage this hunk]
        N[n - Don't stage this hunk]
        S[s - Split into smaller hunks]
        Q[q - Quit]
      end
      
      subgraph "Result"
        Staged[✅ Function A - Staged]
        NotStaged[❌ Function B & C - Not staged]
      end
      
      Change1 --> Interactive
      Change2 --> Interactive
      Change3 --> Interactive
      
      Interactive --> Y
      Interactive --> N
      Interactive --> S
      Interactive --> Q
      
      Y --> Staged
      N --> NotStaged
      
      style Change1 fill:#ffcdd2
      style Change2 fill:#fff3e0
      style Change3 fill:#e3f2fd
      style Interactive fill:#f3e5f5
      style Staged fill:#c8e6c9
      style NotStaged fill:#ffecb3
  `}
/>

## Real-World Example: Preparing a Commit

Let's say you're working on a website and have made several changes:

<Mermaid 
  chart={`
    graph TB
      subgraph "Working Directory Changes"
        WD1[📄 index.html - Added navbar]
        WD2[📄 style.css - Updated colors]
        WD3[📄 about.html - Fixed typos]
        WD4[📄 debug.log - Temporary log file]
        WD5[📄 script.js - Added validation]
      end
      
      subgraph "Selective Staging"
        Decision{What to include?}
      end
      
      subgraph "Staging Area"
        S1[✅ index.html]
        S2[✅ style.css]
        S3[✅ script.js]
        Ignored[❌ debug.log - Don't stage]
        Separate[❌ about.html - Stage separately]
      end
      
      WD1 --> Decision
      WD2 --> Decision
      WD3 --> Decision
      WD4 --> Decision
      WD5 --> Decision
      
      Decision --> S1
      Decision --> S2
      Decision --> S3
      Decision --> Ignored
      Decision --> Separate
      
      style WD1 fill:#e3f2fd
      style WD2 fill:#e3f2fd
      style WD3 fill:#fff3e0
      style WD4 fill:#ffcdd2
      style WD5 fill:#e3f2fd
      style S1 fill:#c8e6c9
      style S2 fill:#c8e6c9
      style S3 fill:#c8e6c9
      style Ignored fill:#ffecb3
      style Separate fill:#fff3e0
  `}
/>

**Commands to stage related changes:**
```bash
# Stage navbar and styling changes together
git add index.html style.css script.js

# Create commit for navbar feature
git commit -m "Add responsive navigation bar"

# Stage typo fixes separately
git add about.html

# Create separate commit for content fixes
git commit -m "Fix typos in about page"
```

## Understanding git add Options

<Mermaid 
  chart={`
    graph TB
      subgraph "git add Variations"
        Basic[git add .]
        All[git add -A]
        Update[git add -u]
        Interactive[git add -i]
        Patch[git add -p]
      end
      
      subgraph "What They Do"
        BasicDesc[Stages new and modified files<br/>in current directory]
        AllDesc[Stages ALL changes<br/>(including deletions)]
        UpdateDesc[Stages only modified<br/>and deleted files]
        InteractiveDesc[Opens interactive menu<br/>for selective staging]
        PatchDesc[Stage parts of files<br/>interactively]
      end
      
      Basic --> BasicDesc
      All --> AllDesc
      Update --> UpdateDesc
      Interactive --> InteractiveDesc
      Patch --> PatchDesc
      
      style Basic fill:#e3f2fd
      style All fill:#e8f5e8
      style Update fill:#fff3e0
      style Interactive fill:#f3e5f5
      style Patch fill:#e1f5fe
      style BasicDesc fill:#f8f9fa
      style AllDesc fill:#f8f9fa
      style UpdateDesc fill:#f8f9fa
      style InteractiveDesc fill:#f8f9fa
      style PatchDesc fill:#f8f9fa
  `}
/>

## Common Workflows and Best Practices

### 1. Check Before You Stage
```bash
git status          # See what files are changed
git diff           # See what changes you made
git add <files>    # Stage specific files
git diff --staged  # Review staged changes
```

<Mermaid 
  chart={`
    graph LR
      Status[git status] --> Review[Review changes]
      Review --> Add[git add files]
      Add --> Verify[git diff --staged]
      Verify --> Commit[git commit]
      
      style Status fill:#e3f2fd
      style Review fill:#fff3e0
      style Add fill:#e8f5e8
      style Verify fill:#f3e5f5
      style Commit fill:#c8e6c9
  `}
/>

### 2. Stage Related Changes Together
Group related changes into logical commits:

<Mermaid 
  chart={`
    graph TB
      subgraph "Changes Made"
        F1[🎨 CSS styling updates]
        F2[🐛 Bug fix in login]
        F3[📝 Update documentation]
        F4[🎨 More CSS changes]
      end
      
      subgraph "Logical Grouping"
        Commit1[Commit 1: UI Styling<br/>CSS updates together]
        Commit2[Commit 2: Bug Fix<br/>Login issue fix]
        Commit3[Commit 3: Documentation<br/>Doc updates]
      end
      
      F1 --> Commit1
      F4 --> Commit1
      F2 --> Commit2
      F3 --> Commit3
      
      style F1 fill:#e1f5fe
      style F2 fill:#ffcdd2
      style F3 fill:#e8f5e8
      style F4 fill:#e1f5fe
      style Commit1 fill:#c8e6c9
      style Commit2 fill:#c8e6c9
      style Commit3 fill:#c8e6c9
  `}
/>

### 3. Avoid Staging Everything Blindly
Be selective about what you stage:

<Mermaid 
  chart={`
    graph TB
      subgraph "❌ Bad Practice"
        BadCommand[git add .]
        BadResult[Stages everything<br/>including unrelated changes]
      end
      
      subgraph "✅ Good Practice"
        GoodCommand[git add specific-files]
        GoodResult[Stages only related changes<br/>for focused commits]
      end
      
      BadCommand --> BadResult
      GoodCommand --> GoodResult
      
      style BadCommand fill:#ffcdd2
      style BadResult fill:#ffcdd2
      style GoodCommand fill:#c8e6c9
      style GoodResult fill:#c8e6c9
  `}
/>

## Troubleshooting Common Issues

### Unstaging Files (Oops, staged wrong file!)
```bash
git reset HEAD <filename>    # Unstage specific file
git reset HEAD .            # Unstage all files
```

<Mermaid 
  chart={`
    graph LR
      Staged[✅ Staged File] -->|git reset HEAD file| Working[📝 Working Directory]
      
      style Staged fill:#c8e6c9
      style Working fill:#fff3e0
  `}
/>

### Checking What's Staged
```bash
git status           # Overview of staged/unstaged files
git diff --staged   # See exact changes that are staged
git diff --cached   # Same as --staged
```

## Staging Area Benefits

<Mermaid 
  chart={`
    mindmap
      root((Staging Area Benefits))
        Selective Commits
          Choose what to commit
          Group related changes
          Create focused commits
          Better project history
        Review Changes
          Double-check before commit
          Catch mistakes early
          Review code quality
          Ensure completeness
        Flexible Workflow
          Stage incrementally
          Partial file staging
          Multiple commits preparation
          Work in progress saves
        Collaboration
          Clear commit messages
          Easier code reviews
          Better team coordination
          Professional workflow
  `}
/>

The staging area is what makes Git powerful - it gives you fine-grained control over what goes into each commit, leading to a cleaner, more meaningful project history.
