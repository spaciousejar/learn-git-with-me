---
title: Git Remote
description: Learn about Git Remote in Git
---
## List remote repositories
```sh
git remote -v
```
This command lists the remote repositories that are associated with the local repository. The `-v` flag shows the URLs of the remote repositories.

## Add a remote repository
```sh
git remote add remote_name remote_url
```
This command adds a new remote repository to the local repository. The `remote_name` is the name of the remote repository, and `remote_url` is the URL of the remote repository.

## Remove a remote repository
```sh
git remote remove remote_name
```
This command removes the remote repository with the name `remote_name` from the local repository.

#### Free Resources

<Tabs defaultValue="official">
<TabsList>
<TabsTrigger value="official">Official Docs</TabsTrigger>
<TabsTrigger value="article">Articles</TabsTrigger>
<TabsTrigger value="video">Videos</TabsTrigger>
</TabsList>

<TabsContent value="official">
<Note type="note" title="official">
- <Link href="https://git-scm.com/docs">Git Documentation</Link>
</Note>
</TabsContent>
<TabsContent value="article">
<Note type="note" title="article">
- <Link href="https://www.atlassian.com/git/tutorials">Atlassian Git Tutorials</Link>
</Note>
</TabsContent>
<TabsContent value="video">
<Note type="note" title="video">
- <Link href="https://www.youtube.com/results?search_query=git+git-remote+tutorial">Video Tutorials</Link>
</Note>
</TabsContent>
</Tabs>
